# Work Packages: Transformer Templates System

**Inputs**: Design documents from `/kitty-specs/005-transformer-templates-system/`
**Prerequisites**: plan.md (required), spec.md (user stories), research.md, data-model.md, contracts/, quickstart.md

**Tests**: Only include explicit testing work when stakeholders request it.

**Organization**: Fine-grained subtasks (`Txxx`) roll up into work packages (`WPxx`). Each work package must be independently deliverable and testable.

**Prompt Files**: Each work package references a matching prompt file in `/tasks/planned/` generated by `/spec-kitty.tasks`. Treat this file as the high-level checklist; keep deep implementation detail inside the prompt files.

## Subtask Format: `[Txxx] [P?] Description`
- **[P]** indicates the subtask can proceed in parallel (different files/components).
- Include precise file paths or modules.

## Path Conventions
- **Single project**: `lib/`, `tests/`
- Adjust paths to match the implementation plan.

---

## Work Package WP01: Setup & Project Structure (Priority: P0)

**Goal**: Establish project structure and module scaffolding for Transformer system.
**Independent Test**: Project structure exists, modules can be imported, basic tests can run.
**Prompt**: `/tasks/planned/WP01-setup-and-project-structure.md`

### Included Subtasks
- [ ] T001 Create module structure: `lib/Qwiratry/Transformer.rakumod`, `lib/Qwiratry/Template.rakumod`, `lib/Qwiratry/Copy.rakumod`
- [ ] T002 [P] Create test structure: `tests/unit/transformer.rakutest`, `tests/unit/template.rakutest`, `tests/unit/copy.rakutest`, `tests/integration/transformer-walker.rakutest`
- [ ] T003 [P] Create exception classes for transformer-specific errors (template ordering conflicts, etc.) in `lib/Qwiratry/X.rakumod`

### Implementation Notes
- Set up basic module files with unit module declarations
- Create test files with basic structure (use Test module)
- Define exception hierarchy for transformer errors

### Parallel Opportunities
- Test structure and exception classes can be created in parallel

### Dependencies
- None (starting package)

### Risks & Mitigations
- Module naming conflicts → follow existing Qwiratry module naming conventions

---

## Work Package WP02: Transformer Declarator Implementation (Priority: P1) 🎯 MVP

**Goal**: Implement `transformer` custom declarator using `EXPORTHOW::DECLARE` mechanism.
**Independent Test**: Can declare a transformer with `transformer MyTransform { }` and it creates a callable class.
**Prompt**: `/tasks/planned/WP02-transformer-declarator.md`

### Included Subtasks
- [ ] T004 Create `MetamodelX::TransformerHOW` class extending `Metamodel::ClassHOW` in `lib/Qwiratry/Transformer.rakumod`
- [ ] T005 Implement `EXPORTHOW::DECLARE` export: `my package EXPORTHOW { package DECLARE { constant transformer = MetamodelX::TransformerHOW; } }`
- [ ] T006 Implement HOW class to process transformer body and collect templates/wrappers during compilation
- [ ] T007 Create callable sub/method with transformer name that invokes `TRANSFORM` method
- [ ] T008 [P] Add unit tests for transformer declarator: declaration, basic structure, callable behavior

### Implementation Notes
- Follow Red ORM pattern for `EXPORTHOW::DECLARE` implementation
- HOW class needs to traverse body AST to collect templates and wrappers
- Transformer class should store templates in `@.templates` attribute
- Callable method should be created automatically

### Parallel Opportunities
- Unit tests can be written in parallel with implementation

### Dependencies
- Depends on WP01

### Risks & Mitigations
- HOW class complexity → start with minimal implementation, extend incrementally
- AST traversal complexity → use existing Raku AST introspection capabilities

---

## Work Package WP03: Template Declarator & Collection (Priority: P1) 🎯 MVP

**Goal**: Parse `template` declarations within transformer body and collect template metadata.
**Independent Test**: Can declare templates within transformer, templates are collected and stored correctly.
**Prompt**: `/tasks/planned/WP03-template-declarator-and-collection.md`

### Included Subtasks
- [ ] T009 Implement template parsing in HOW class: identify `template` declarations in transformer body AST
- [ ] T010 Extract template components: name, signature, traits, `when` block, `do` block
- [ ] T011 Create `Template` class in `lib/Qwiratry/Template.rakumod` with attributes: name, signature, when-block, do-block, priority, specificity, tie-breaker, streaming, returns-type
- [ ] T012 Store templates in transformer's `@.templates` array during compilation
- [ ] T013 [P] Add unit tests for template declarator: declaration, component extraction, storage

### Implementation Notes
- Manual AST traversal to find template declarations
- Template class should store all metadata needed for ordering and execution
- Named templates should become callable methods on transformer class

### Parallel Opportunities
- Template class creation and unit tests can proceed in parallel

### Dependencies
- Depends on WP02

### Risks & Mitigations
- AST parsing complexity → use pattern matching on AST node types
- Template signature parsing → leverage Raku's Signature parsing

---

## Work Package WP04: Template Ordering Algorithm (Priority: P1) 🎯 MVP

**Goal**: Implement `ORDER-TEMPLATES` method that sorts templates by priority → specificity → tie-breaker.
**Independent Test**: Templates with different priorities are ordered correctly, conflicts are detected and reported.
**Prompt**: `/tasks/planned/WP04-template-ordering-algorithm.md`

### Included Subtasks
- [ ] T014 Implement priority sorting: extract `:priority` trait (default 0), sort highest first
- [ ] T015 Implement basic specificity calculation: analyze `when` clause AST for static patterns (axis operators, wildcards, path elements)
- [ ] T016 Implement specificity scoring: multilevel axis (-100), wildcards (-10), explicit path elements (+5), attribute axes (+5)
- [ ] T017 Implement tie-breaker resolution: extract `:tie-breaker` trait (default 0), sort highest first
- [ ] T018 Implement conflict detection: report error when two templates have equal priority/specificity/tie-breaker and could match same node
- [ ] T019 Implement `ORDER-TEMPLATES` method that populates `@.ordered-templates` array
- [ ] T020 [P] Add unit tests for template ordering: priority sorting, specificity calculation, tie-breaker, conflict detection

### Implementation Notes
- Hybrid approach: calculate static aspects at compile time, defer complex cases to runtime
- Cache ordering results to avoid recalculation
- Specificity calculation may need refinement based on query operator implementations

### Parallel Opportunities
- Unit tests can be written in parallel with implementation

### Dependencies
- Depends on WP03

### Risks & Mitigations
- Specificity calculation complexity → start with basic cases, extend for complex queries
- Conflict detection accuracy → use conservative approach (report conflict if uncertain)

---

## Work Package WP05: Magic Variables & Template Execution (Priority: P1) 🎯 MVP

**Goal**: Set magic variables (`$*CONTEXT`, `$*CAPTURE`, `self`) during template execution and implement basic template matching.
**Independent Test**: Magic variables are correctly set and accessible in template actions, templates match nodes correctly.
**Prompt**: `/tasks/planned/WP05-magic-variables-and-template-execution.md`

### Included Subtasks
- [ ] T021 Implement `$*CONTEXT` and `$_` setup: set to current node before template execution
- [ ] T022 Implement `$*CAPTURE` and `$/` setup: set to template signature parameters if template has signature
- [ ] T023 Ensure `self` refers to Transformer object (automatic in Raku methods/blocks)
- [ ] T024 Implement `Template.matches($node --> Bool)` method: evaluate `when` block with magic variables set
- [ ] T025 Implement `Template.execute($node, :$context --> Iterator|Mu|List|Nil)` method: execute `do` block with magic variables set
- [ ] T026 [P] Add unit tests for magic variables: `$*CONTEXT`, `$*CAPTURE`, `self` availability and correctness

### Implementation Notes
- Use Raku dynamic variables (twigil `*`) for `$*CONTEXT` and `$*CAPTURE`
- Set variables before executing template blocks, scope to template execution
- Magic variables should not leak between templates

### Parallel Opportunities
- Unit tests can be written in parallel with implementation

### Dependencies
- Depends on WP03, WP04

### Risks & Mitigations
- Dynamic variable scoping → ensure proper lexical scoping
- Variable leakage → use proper scope boundaries

---

## Work Package WP06: APPLY & TRANSFORM Methods (Priority: P1) 🎯 MVP

**Goal**: Implement core transformation methods: `APPLY` for single node, `TRANSFORM` for full transformation with Walker integration.
**Independent Test**: Can transform a data structure using a transformer with templates, produces correct output.
**Prompt**: `/tasks/planned/WP06-apply-and-transform-methods.md`

### Included Subtasks
- [ ] T027 Implement `APPLY($node --> Iterator|Mu|List|Nil)` method: iterate through `@.ordered-templates`, find first match, execute template, return result
- [ ] T028 Implement Walker factory/registry pattern: `WalkerFactory.get-walker($data --> Walker?)` in `lib/Qwiratry/WalkerFactory.rakumod` (or extend existing)
- [ ] T029 Implement `TRANSFORM($data, Iterator :$iterator --> Iterator|Mu|List|Nil)` method: call `ORDER-TEMPLATES`, obtain Walker, iterate data, call `APPLY` for each node
- [ ] T030 Implement default iterator (depth-first, top-down) or use Walker-provided iterator
- [ ] T031 Implement `transform($input, :$context, :$streaming, :$mode --> Iterator|Mu|List|Nil)` method: determine mode, delegate to appropriate method
- [ ] T032 [P] Add integration tests: transformer with Walker, end-to-end transformations

### Implementation Notes
- `APPLY` should stop after first matching template (no fallback)
- Walker integration should use factory pattern for automatic selection
- Default mode should auto-detect based on input type

### Parallel Opportunities
- Walker factory and integration tests can proceed in parallel

### Dependencies
- Depends on WP04, WP05
- Requires existing Walker system (feature 002)

### Risks & Mitigations
- Walker integration complexity → start with simple Walker, extend for complex cases
- Iterator coordination → ensure proper state management

---

## Work Package WP07: Copy Service Class (Priority: P2)

**Goal**: Implement `Qwiratry::Copy` service class with `copy()` and `deepcopy()` multi subs, attach methods to Transformer object.
**Independent Test**: Can call `Qwiratry::Copy::copy()` and `Qwiratry::Copy::deepcopy()` on transformable nodes, produces correct copies.
**Prompt**: `/tasks/planned/WP07-copy-service-class.md`

### Included Subtasks
- [ ] T033 Create `Qwiratry::Copy` module in `lib/Qwiratry/Copy.rakumod`
- [ ] T034 Implement `multi sub copy(Mu $x --> Mu)`: default identity
- [ ] T035 Implement `multi sub copy(Positional $p --> Positional)`: shallow copy via `clone`
- [ ] T036 Implement `multi sub copy(Associative $a --> Associative)`: shallow copy via `clone`
- [ ] T037 Implement `multi sub deepcopy(Mu $x --> Mu)`: default identity (atoms, objects with identity)
- [ ] T038 Implement `multi sub deepcopy(Positional $p --> Positional)`: recursive deep copy via `map`
- [ ] T039 Implement `multi sub deepcopy(Associative $a --> Associative)`: recursive deep copy via `map` with cycle detection
- [ ] T040 Implement cycle detection for `deepcopy`: maintain "visited" hash keyed by object identity
- [ ] T041 Implement DAG preservation: single clone per unique node regardless of parent count
- [ ] T042 Check for custom `.copy()` method on nodes before using default implementation
- [ ] T043 Attach `copy()` and `deepcopy()` methods to Transformer object (delegate to service functions)
- [ ] T044 [P] Add unit tests for Copy service: shallow copy, deep copy, DAG handling, cycle detection, custom method detection

### Implementation Notes
- Follow spec section 3.3.6 exactly
- Default implementations for Positional and Associative types
- Cycle detection requires visited hash with object identity keys
- Methods attached to Transformer for convenient access

### Parallel Opportunities
- Different multi subs can be implemented in parallel
- Unit tests can be written in parallel

### Dependencies
- Depends on WP02

### Risks & Mitigations
- Cycle detection complexity → use standard visited hash pattern
- DAG preservation → maintain identity-to-clone mapping

---

## Work Package WP08: Wrapper System (Priority: P3)

**Goal**: Implement wrapper system (TRANSFORMER, TEMPLATE_MATCHER, TEMPLATE_ACTION) as submethods called up hierarchy.
**Independent Test**: Can define wrappers in transformer, wrappers execute at appropriate points during transformation.
**Prompt**: `/tasks/planned/WP08-wrapper-system.md`

### Included Subtasks
- [ ] T045 Parse `wrapper` declarations in transformer body (similar to template parsing)
- [ ] T046 Create submethods: `WRAP_TRANSFORMER`, `WRAP_TEMPLATE_MATCHER`, `WRAP_TEMPLATE_ACTION`
- [ ] T047 Implement wrapper call mechanism: call up transformer hierarchy (like `TWEAK`)
- [ ] T048 Implement `WRAP_TRANSFORMER` execution: wrap entire `TRANSFORM` method output
- [ ] T049 Implement `WRAP_TEMPLATE_MATCHER` execution: wrap template `when` block evaluation
- [ ] T050 Implement `WRAP_TEMPLATE_ACTION` execution: wrap template `do` block execution
- [ ] T051 [P] Add unit tests for wrapper system: all three wrapper types, hierarchy traversal

### Implementation Notes
- Wrappers are submethods, called via MRO traversal
- Wrappers receive appropriate parameters (varies by type)
- Use `callwith` or similar for hierarchy traversal

### Parallel Opportunities
- Different wrapper types can be implemented in parallel
- Unit tests can be written in parallel

### Dependencies
- Depends on WP06

### Risks & Mitigations
- Submethod hierarchy complexity → follow Raku's standard submethod mechanism
- Parameter passing → ensure correct parameters for each wrapper type

---

## Work Package WP09: Traits & Advanced Features (Priority: P2)

**Goal**: Implement trait support (`:streaming`, `returns(Type)`, `does TreeRewrite`) and transformation modes (pre, inline, post).
**Independent Test**: Can use traits on transformers and templates, transformation modes work correctly.
**Prompt**: `/tasks/planned/WP09-traits-and-advanced-features.md`

### Included Subtasks
- [ ] T052 Implement `:streaming` trait: enable lazy iterator-based output using `gather/take`
- [ ] T053 Implement `returns(Type)` trait: enforce output type checking for transformers and templates
- [ ] T054 Implement `does TreeRewrite` role: override `APPLY` method for in-place rewriting
- [ ] T055 Implement `prepare($data, :$ctx)` method: pre-transformation stage
- [ ] T056 Implement `apply($element, :$ctx, :$mode)` method: inline transformation stage
- [ ] T057 Implement transformation mode detection: auto-detect based on input type in `transform` method
- [ ] T058 Implement `rewrite-optional` and `rewrite-mandatory` modes
- [ ] T059 [P] Add unit tests for traits: streaming, returns, TreeRewrite
- [ ] T060 [P] Add unit tests for transformation modes: pre, inline, post, rewrite modes

### Implementation Notes
- Traits applied at compile-time, inspectable at runtime
- Streaming uses `gather/take` for lazy evaluation
- TreeRewrite modifies APPLY behavior for in-place mutation
- Transformation modes determine which internal method to call

### Parallel Opportunities
- Different traits can be implemented in parallel
- Transformation modes and unit tests can proceed in parallel

### Dependencies
- Depends on WP06

### Risks & Mitigations
- Trait introspection complexity → use Raku's `.^traits` mechanism
- Streaming coordination → ensure proper iterator handling

---

## Work Package WP10: Integration & Polish (Priority: P2)

**Goal**: Integration tests, documentation updates, and final polish.
**Independent Test**: All integration tests pass, quickstart examples work, documentation is complete.
**Prompt**: `/tasks/planned/WP10-integration-and-polish.md`

### Included Subtasks
- [ ] T061 [P] Add integration tests: transformer with Strategy system, multi-phase transformations
- [ ] T062 [P] Validate quickstart.md examples: ensure all examples work correctly
- [ ] T063 [P] Update documentation: ensure all features are documented
- [ ] T064 [P] Code cleanup and refactoring: improve code quality, remove dead code
- [ ] T065 [P] Performance optimization: ensure template ordering is efficient, streaming works correctly
- [ ] T066 [P] Error message improvements: ensure diagnostic messages are clear and actionable

### Implementation Notes
- Focus on end-to-end scenarios
- Ensure all user stories can be demonstrated
- Validate against success criteria

### Parallel Opportunities
- All subtasks can proceed in parallel once core features are complete

### Dependencies
- Depends on WP06, WP07, WP08, WP09

### Risks & Mitigations
- Integration complexity → test incrementally
- Performance issues → profile and optimize hotspots

---

## Dependency & Execution Summary

- **Sequence**: WP01 → WP02 → WP03 → WP04 → WP05 → WP06 → WP07 → WP08 → WP09 → WP10
- **Parallelization**: 
  - Within packages: Test writing can proceed in parallel with implementation
  - WP07 (Copy) can start after WP02
  - WP08 (Wrappers) and WP09 (Traits) can proceed in parallel after WP06
  - WP10 can start once core features (WP01-WP06) are complete
- **MVP Scope**: WP01-WP06 constitute the minimal viable product (basic transformer functionality)

---

## Subtask Index (Reference)

| Subtask ID | Summary | Work Package | Priority | Parallel? |
|------------|---------|--------------|----------|-----------|
| T001       | Create module structure | WP01 | P0 | No |
| T002       | Create test structure | WP01 | P0 | Yes |
| T003       | Create exception classes | WP01 | P0 | Yes |
| T004       | Create TransformerHOW class | WP02 | P1 | No |
| T005       | Implement EXPORTHOW::DECLARE | WP02 | P1 | No |
| T006       | Process transformer body | WP02 | P1 | No |
| T007       | Create callable method | WP02 | P1 | No |
| T008       | Unit tests for declarator | WP02 | P1 | Yes |
| T009       | Parse template declarations | WP03 | P1 | No |
| T010       | Extract template components | WP03 | P1 | No |
| T011       | Create Template class | WP03 | P1 | No |
| T012       | Store templates | WP03 | P1 | No |
| T013       | Unit tests for templates | WP03 | P1 | Yes |
| T014       | Priority sorting | WP04 | P1 | No |
| T015       | Basic specificity calculation | WP04 | P1 | No |
| T016       | Specificity scoring | WP04 | P1 | No |
| T017       | Tie-breaker resolution | WP04 | P1 | No |
| T018       | Conflict detection | WP04 | P1 | No |
| T019       | ORDER-TEMPLATES method | WP04 | P1 | No |
| T020       | Unit tests for ordering | WP04 | P1 | Yes |
| T021       | $*CONTEXT setup | WP05 | P1 | No |
| T022       | $*CAPTURE setup | WP05 | P1 | No |
| T023       | self reference | WP05 | P1 | No |
| T024       | Template.matches method | WP05 | P1 | No |
| T025       | Template.execute method | WP05 | P1 | No |
| T026       | Unit tests for magic variables | WP05 | P1 | Yes |
| T027       | APPLY method | WP06 | P1 | No |
| T028       | Walker factory | WP06 | P1 | No |
| T029       | TRANSFORM method | WP06 | P1 | No |
| T030       | Default iterator | WP06 | P1 | No |
| T031       | transform method | WP06 | P1 | No |
| T032       | Integration tests | WP06 | P1 | Yes |
| T033       | Create Copy module | WP07 | P2 | No |
| T034       | copy(Mu) multi sub | WP07 | P2 | No |
| T035       | copy(Positional) multi sub | WP07 | P2 | No |
| T036       | copy(Associative) multi sub | WP07 | P2 | No |
| T037       | deepcopy(Mu) multi sub | WP07 | P2 | No |
| T038       | deepcopy(Positional) multi sub | WP07 | P2 | No |
| T039       | deepcopy(Associative) multi sub | WP07 | P2 | No |
| T040       | Cycle detection | WP07 | P2 | No |
| T041       | DAG preservation | WP07 | P2 | No |
| T042       | Custom method check | WP07 | P2 | No |
| T043       | Attach methods to Transformer | WP07 | P2 | No |
| T044       | Unit tests for Copy | WP07 | P2 | Yes |
| T045       | Parse wrapper declarations | WP08 | P3 | No |
| T046       | Create wrapper submethods | WP08 | P3 | No |
| T047       | Wrapper call mechanism | WP08 | P3 | No |
| T048       | WRAP_TRANSFORMER execution | WP08 | P3 | No |
| T049       | WRAP_TEMPLATE_MATCHER execution | WP08 | P3 | No |
| T050       | WRAP_TEMPLATE_ACTION execution | WP08 | P3 | No |
| T051       | Unit tests for wrappers | WP08 | P3 | Yes |
| T052       | :streaming trait | WP09 | P2 | No |
| T053       | returns(Type) trait | WP09 | P2 | No |
| T054       | does TreeRewrite role | WP09 | P2 | No |
| T055       | prepare method | WP09 | P2 | No |
| T056       | apply method (inline) | WP09 | P2 | No |
| T057       | Mode detection | WP09 | P2 | No |
| T058       | Rewrite modes | WP09 | P2 | No |
| T059       | Unit tests for traits | WP09 | P2 | Yes |
| T060       | Unit tests for modes | WP09 | P2 | Yes |
| T061       | Integration tests | WP10 | P2 | Yes |
| T062       | Validate quickstart | WP10 | P2 | Yes |
| T063       | Update documentation | WP10 | P2 | Yes |
| T064       | Code cleanup | WP10 | P2 | Yes |
| T065       | Performance optimization | WP10 | P2 | Yes |
| T066       | Error message improvements | WP10 | P2 | Yes |
