# Work Packages: Composite Walker Handovers

**Inputs**: Design documents from `/kitty-specs/004-composite-walker-handovers/`
**Prerequisites**: plan.md (required), spec.md (user stories), research.md, data-model.md, contracts/, quickstart.md

**Tests**: Only include explicit testing work when stakeholders request it.

**Organization**: Fine-grained subtasks (`Txxx`) roll up into work packages (`WPxx`). Each work package must be independently deliverable and testable.

**Prompt Files**: Each work package references a matching prompt file in `/tasks/planned/` generated by `/spec-kitty.tasks`. Treat this file as the high-level checklist; keep deep implementation detail inside the prompt files.

## Subtask Format: `[Txxx] [P?] Description`
- **[P]** indicates the subtask can proceed in parallel (different files/components).
- Include precise file paths or modules.

## Path Conventions
- **Single project**: `lib/Qwiratry/`, `tests/`
- Adjust paths to match the implementation plan.

---

## Work Package WP01: Setup & Project Structure (Priority: P0)

**Goal**: Establish project structure and module organization for composite walker handovers feature.
**Independent Test**: Project structure exists with placeholder modules, tests can be run.
**Prompt**: `/tasks/planned/WP01-setup-and-project-structure.md`

### Included Subtasks
- [x] T001 Create module structure: `lib/Qwiratry/Provides.rakumod`, `lib/Qwiratry/MasterWalker.rakumod`
- [x] T002 [P] Create test structure: `tests/unit/provides.rakutest`, `tests/unit/master-walker.rakutest`, `tests/integration/composite-handover.rakutest`
- [x] T003 Verify Walker core infrastructure (feature 002) is available and accessible

### Implementation Notes
- Create module files with basic structure (unit module declarations, exports)
- Create test files with basic test structure (use Test module)
- Verify dependencies on Walker role, Walker::Plan role, Context role, QueryIterator role

### Parallel Opportunities
- Module creation and test structure creation can proceed in parallel

### Dependencies
- None (starting package, but assumes feature 002 is complete)

### Risks & Mitigations
- Missing dependencies from feature 002 → verify Walker infrastructure exists before proceeding

---

## Work Package WP02: provides Trait Implementation (Priority: P1) 🎯 MVP

**Goal**: Implement `provides<domain-name>` compile-time trait that attaches advisory domain metadata to root objects.
**Independent Test**: Can apply `provides<sql>` trait to variable declaration, metadata is discoverable at runtime via `.^traits` or `.^meta`.
**Prompt**: `/tasks/planned/WP02-provides-trait-implementation.md`

### Included Subtasks
- [x] T004 Implement `trait_mod:<provides>` subroutine in `lib/Qwiratry/Provides.rakumod`
- [x] T005 Store domain metadata in meta-object (Array[Str] of domain names)
- [x] T006 Implement runtime discovery mechanism (access via `.^traits` or `.^meta` introspection)
- [x] T007 [P] Unit tests: trait application to variable declarations in `tests/unit/provides.rakutest`
- [ ] T008 [P] Unit tests: metadata discovery for single domain (`provides<sql>`)
- [ ] T009 [P] Unit tests: metadata discovery for multiple domains (`provides<sql json>`)
- [ ] T010 [P] Unit tests: verify trait does not alter runtime semantics or method dispatch

### Implementation Notes
- Implement `trait_mod:<provides>` that accepts domain names and attaches to declarand's meta-object
- Use Raku meta-object protocol to store metadata (e.g., via custom meta-object attributes or `.^traits`)
- Ensure metadata is discoverable by Slangs and Walkers during planning phase
- Tests should verify compile-time application and runtime discovery

### Parallel Opportunities
- Test cases can be written in parallel once trait implementation exists

### Dependencies
- Depends on WP01.

### Risks & Mitigations
- Meta-object protocol complexity → research Raku trait storage mechanisms, use standard patterns

---

## Work Package WP03: Master Walker Discovery & Registration (Priority: P1) 🎯 MVP

**Goal**: Implement Master Walker class with walker discovery (default) and explicit registration (override) mechanisms.
**Independent Test**: Master Walker can discover candidate walkers via introspection, or accept explicit list via constructor parameter.
**Prompt**: `/tasks/planned/WP03-master-walker-discovery-and-registration.md`

### Included Subtasks
- [ ] T011 Implement `Qwiratry::MasterWalker` class in `lib/Qwiratry/MasterWalker.rakumod` (basic structure, implements Walker role)
- [ ] T012 Implement `discover-walkers()` method: scan loaded classes/types checking `$type.^does(Walker)`
- [ ] T013 Implement lazy discovery caching (cache discovered walkers per instance)
- [ ] T014 Implement constructor with optional `:@candidate-walkers` parameter (overrides discovery)
- [ ] T015 [P] Unit tests: discovery mechanism finds walkers implementing Walker role in `tests/unit/master-walker.rakutest`
- [ ] T016 [P] Unit tests: explicit registration overrides discovery
- [ ] T017 [P] Unit tests: discovery caching (only runs once per instance)

### Implementation Notes
- Master Walker must implement Walker role (plan, iterator methods)
- Discovery scans loaded modules/classes for those implementing Walker role
- Cache discovered walkers to avoid repeated introspection
- When explicit candidate walkers provided, skip discovery and use provided list

### Parallel Opportunities
- Test cases can be written in parallel once basic structure exists

### Dependencies
- Depends on WP01, requires Walker role from feature 002.

### Risks & Mitigations
- Discovery performance → cache results per instance, lazy initialization
- Discovery may find unwanted walkers → allow explicit registration to filter

---

## Work Package WP04: Handover Detection - Domain Metadata & Capability Checks (Priority: P1) 🎯 MVP

**Goal**: Implement handover detection using domain metadata (fast path) and capability checks (fallback), enabling Master Walker to delegate to domain-specific walkers.
**Independent Test**: Master Walker detects handover requirements via domain metadata or capability checks, delegates planning to appropriate walker.
**Prompt**: `/tasks/planned/WP04-handover-detection-domain-metadata-and-capability.md`

### Included Subtasks
- [ ] T018 Implement `check-domain-metadata(Mu $root --> Array[Str]?)` method: checks `provides` trait, returns domain names or Nil
- [ ] T019 Implement domain metadata fast path: if domains declared, find walker supporting at least one domain
- [ ] T020 Implement early failure: if domain metadata declares domains but no suitable walker exists, fail with diagnostic error
- [ ] T021 Implement `check-capability(RakuAST::Node $subtree, Walker $walker --> Bool)` method: calls `$walker.supports($subtree)`
- [ ] T022 Implement capability check fallback: query candidate walkers via `supports($subtree)`, delegate to first returning True
- [ ] T023 Implement `detect-handover(RakuAST::Node $subtree, Mu $root --> Walker?)` method: combines domain metadata and capability checks
- [ ] T024 [P] Unit tests: domain metadata check finds suitable walker in `tests/unit/master-walker.rakutest`
- [ ] T025 [P] Unit tests: domain metadata check fails early when no suitable walker exists
- [ ] T026 [P] Unit tests: capability check queries walkers via `supports()`
- [ ] T027 [P] Unit tests: capability check delegates to first walker returning True

### Implementation Notes
- Handover detection follows priority: domain metadata (fast path) → capability checks (fallback)
- Domain metadata check: extract domains from `provides` trait, find walker supporting at least one domain
- Capability check: query each candidate walker via `supports($subtree)`, delegate to first returning True
- If all checks fail, return Nil (handover not possible)
- Early failure when domain metadata declares domains but no suitable walker exists

### Parallel Opportunities
- Test cases can be written in parallel once detection methods exist

### Dependencies
- Depends on WP02 (provides trait), WP03 (Master Walker structure).

### Risks & Mitigations
- Multiple walkers support same subtree → select first one found (can be enhanced later with priority)
- Walker `supports()` returns True but cannot execute → walker will throw exception during planning (acceptable)

---

## Work Package WP05: Plan-Level Handover with Embedded Subplans (Priority: P1) 🎯 MVP

**Goal**: Implement plan-level handover where Master Walker delegates planning to domain-specific walkers and embeds resulting plans as subplans.
**Independent Test**: Master Walker detects handover, delegates planning to domain-specific walker, receives Walker::Plan, embeds as subplan in composite plan.
**Prompt**: `/tasks/planned/WP05-plan-level-handover-with-subplans.md`

### Included Subtasks
- [ ] T028 Implement `CompositePlan` class in `lib/Qwiratry/MasterWalker.rakumod` (or separate module) implementing `Walker::Plan` role
- [ ] T029 Implement `CompositePlan` attributes: `$.query-ast`, `@.subplans`, execution metadata
- [ ] T030 Implement `CompositePlan.subplans()` method: returns `Array[Walker::Plan]` of embedded subplans
- [ ] T031 Implement `MasterWalker.plan()` method: detects handovers, delegates planning, embeds subplans
- [ ] T032 Implement AST subtree extraction: extract relevant subtree from query for delegation
- [ ] T033 Implement delegation: call `$walker.plan($subtree, $root)` and receive `Walker::Plan`
- [ ] T034 Implement subplan embedding: add delegated plan to `CompositePlan.subplans` array
- [ ] T035 [P] Unit tests: handover detection triggers delegation in `tests/unit/master-walker.rakutest`
- [ ] T036 [P] Unit tests: composite plan contains embedded subplans
- [ ] T037 [P] Unit tests: composite plan.query() returns original query AST (not modified)
- [ ] T038 [P] Unit tests: plan immutability (subplans don't mutate original query AST)

### Implementation Notes
- CompositePlan implements Walker::Plan role with subplans array
- MasterWalker.plan() detects handovers, extracts subtrees, delegates to domain-specific walkers
- Embed delegated plans as subplans in CompositePlan
- Ensure plan immutability: don't mutate original query AST
- CompositePlan.query() returns original composite query AST

### Parallel Opportunities
- Test cases can be written in parallel once CompositePlan structure exists

### Dependencies
- Depends on WP04 (handover detection).

### Risks & Mitigations
- Subtree extraction complexity → ensure correct AST boundaries, validate extracted subtrees
- Plan immutability → ensure delegated walkers don't mutate original query AST

---

## Work Package WP06: Composite Execution Coordination (Priority: P2)

**Goal**: Implement composite execution coordination where Master Walker orchestrates execution of multiple subplans, coordinating data flow and result materialization.
**Independent Test**: Composite plan with multiple subplans executes correctly, results are combined, data flows between domains as expected.
**Prompt**: `/tasks/planned/WP06-composite-execution-coordination.md`

### Included Subtasks
- [ ] T039 Implement `CompositePlan.iterator()` method: creates composite iterator
- [ ] T040 Implement composite iterator: coordinates subplan iterators in execution order
- [ ] T041 Implement execution ordering: determine order during planning (dependency analysis or explicit order)
- [ ] T042 Implement data flow: materialize results from one subplan before feeding to next (for MVP)
- [ ] T043 Implement result combination: combine results from subplans according to query structure
- [ ] T044 [P] Integration tests: composite plan execution with multiple subplans in `tests/integration/composite-handover.rakutest`
- [ ] T045 [P] Integration tests: execution ordering is correct
- [ ] T046 [P] Integration tests: data flow between domains works correctly

### Implementation Notes
- CompositePlan.iterator() creates composite iterator that coordinates subplan iterators
- Execution ordering determined during planning phase
- For MVP: materialize results from subplans (streaming coordination deferred to future)
- Combine results according to query structure (join semantics deferred to future enhancement)

### Parallel Opportunities
- Test cases can be written in parallel once composite iterator exists

### Dependencies
- Depends on WP05 (plan-level handover).

### Risks & Mitigations
- Result materialization memory usage → for MVP, materialize; streaming can be added later
- Join semantics complexity → defer to future enhancement, focus on basic coordination first

---

## Work Package WP07: Handover Detection Priority Order & Edge Cases (Priority: P2)

**Goal**: Complete handover detection priority order implementation (AST pattern, heuristics) and handle edge cases.
**Independent Test**: Master Walker follows priority order correctly, handles edge cases (no walker found, multiple walkers support subtree, walker declines responsibility).
**Prompt**: `/tasks/planned/WP07-handover-detection-priority-and-edge-cases.md`

### Included Subtasks
- [ ] T047 Implement AST pattern suitability check (optional optimization): recognize AST patterns, delegate if pattern matches
- [ ] T048 Implement heuristic probing (optional, last resort): use heuristics to select walker
- [ ] T049 Ensure priority order: domain metadata → capability → pattern → heuristic
- [ ] T050 Handle edge case: no walker supports required subtree → fail with diagnostic error
- [ ] T051 Handle edge case: multiple walkers support subtree → select first one (or based on priority/specificity)
- [ ] T052 Handle edge case: walker declines responsibility after accepting via `supports()` → walker throws exception during planning
- [ ] T053 [P] Integration tests: priority order is followed correctly in `tests/integration/composite-handover.rakutest`
- [ ] T054 [P] Integration tests: edge cases handled correctly

### Implementation Notes
- AST pattern suitability is optional optimization (not required for correctness)
- Heuristic probing is last resort (optional, can be skipped for MVP)
- Ensure strict priority order: try domain metadata first, then capability, then pattern, then heuristic
- Handle edge cases gracefully with diagnostic errors

### Parallel Opportunities
- Test cases can be written in parallel once edge case handling exists

### Dependencies
- Depends on WP04 (handover detection).

### Risks & Mitigations
- AST pattern recognition complexity → make optional, can be enhanced later
- Heuristic accuracy → make last resort, can be improved based on usage patterns

---

## Work Package WP08: Integration Tests & Polish (Priority: P2)

**Goal**: Complete end-to-end integration tests and polish the implementation.
**Independent Test**: All integration tests pass, documentation updated, code follows Raku style guidelines.
**Prompt**: `/tasks/planned/WP08-integration-tests-and-polish.md`

### Included Subtasks
- [ ] T055 [P] Integration tests: end-to-end multi-domain query planning and execution in `tests/integration/composite-handover.rakutest`
- [ ] T056 [P] Integration tests: composite plan with subplans from different walkers executes correctly
- [ ] T057 [P] Integration tests: handover detection works with real domain-specific walkers (if available)
- [ ] T058 Code cleanup and refactoring: ensure Raku coding style (Tim Nelson/Elizabeth Mattijsen style)
- [ ] T059 Documentation updates: ensure Rakudoc comments in all modules
- [ ] T060 Validate quickstart.md scenario (if quickstart exists)
- [ ] T061 Performance validation: verify trait metadata discovery is efficient, walker discovery is cached

### Implementation Notes
- Write comprehensive integration tests covering end-to-end flows
- Ensure code follows Raku style guidelines (Tim Nelson/Elizabeth Mattijsen style)
- Add Rakudoc comments to all public methods and classes
- Validate performance goals are met (trait discovery O(1) or O(n), walker discovery cached)

### Parallel Opportunities
- Integration tests can be written in parallel
- Code cleanup and documentation can proceed in parallel

### Dependencies
- Depends on completion of WP02-WP07.

### Risks & Mitigations
- Missing domain-specific walkers for integration tests → create mock walkers or use existing ones from feature 002

---

## Dependency & Execution Summary

- **Sequence**: WP01 → WP02 → WP03 → WP04 → WP05 → WP06 → WP07 → WP08
- **Parallelization**: 
  - Within WP02: test cases can be written in parallel
  - Within WP03: test cases can be written in parallel
  - Within WP04: test cases can be written in parallel
  - Within WP05: test cases can be written in parallel
  - Within WP06: test cases can be written in parallel
  - Within WP07: test cases can be written in parallel
  - Within WP08: integration tests, code cleanup, and documentation can proceed in parallel
- **MVP Scope**: WP01-WP05 constitute the minimal release (provides trait, Master Walker discovery, handover detection, plan-level handover with subplans). WP06-WP08 are enhancements (composite execution coordination, priority order edge cases, polish).

---

## Subtask Index (Reference)

| Subtask ID | Summary | Work Package | Priority | Parallel? |
|------------|---------|--------------|----------|-----------|
| T001       | Create module structure | WP01 | P0 | No |
| T002       | Create test structure | WP01 | P0 | Yes |
| T003       | Verify Walker infrastructure | WP01 | P0 | No |
| T004       | Implement trait_mod:<provides> | WP02 | P1 | No |
| T005       | Store domain metadata in meta-object | WP02 | P1 | No |
| T006       | Implement runtime discovery mechanism | WP02 | P1 | No |
| T007       | Unit tests: trait application | WP02 | P1 | Yes |
| T008       | Unit tests: single domain discovery | WP02 | P1 | Yes |
| T009       | Unit tests: multiple domain discovery | WP02 | P1 | Yes |
| T010       | Unit tests: trait runtime semantics | WP02 | P1 | Yes |
| T011       | Implement MasterWalker class structure | WP03 | P1 | No |
| T012       | Implement discover-walkers() method | WP03 | P1 | No |
| T013       | Implement lazy discovery caching | WP03 | P1 | No |
| T014       | Implement constructor with candidate-walkers | WP03 | P1 | No |
| T015       | Unit tests: discovery mechanism | WP03 | P1 | Yes |
| T016       | Unit tests: explicit registration override | WP03 | P1 | Yes |
| T017       | Unit tests: discovery caching | WP03 | P1 | Yes |
| T018       | Implement check-domain-metadata() | WP04 | P1 | No |
| T019       | Implement domain metadata fast path | WP04 | P1 | No |
| T020       | Implement early failure for missing walkers | WP04 | P1 | No |
| T021       | Implement check-capability() | WP04 | P1 | No |
| T022       | Implement capability check fallback | WP04 | P1 | No |
| T023       | Implement detect-handover() | WP04 | P1 | No |
| T024       | Unit tests: domain metadata check | WP04 | P1 | Yes |
| T025       | Unit tests: early failure | WP04 | P1 | Yes |
| T026       | Unit tests: capability check | WP04 | P1 | Yes |
| T027       | Unit tests: capability delegation | WP04 | P1 | Yes |
| T028       | Implement CompositePlan class | WP05 | P1 | No |
| T029       | Implement CompositePlan attributes | WP05 | P1 | No |
| T030       | Implement CompositePlan.subplans() | WP05 | P1 | No |
| T031       | Implement MasterWalker.plan() | WP05 | P1 | No |
| T032       | Implement AST subtree extraction | WP05 | P1 | No |
| T033       | Implement delegation to walkers | WP05 | P1 | No |
| T034       | Implement subplan embedding | WP05 | P1 | No |
| T035       | Unit tests: handover delegation | WP05 | P1 | Yes |
| T036       | Unit tests: composite plan subplans | WP05 | P1 | Yes |
| T037       | Unit tests: composite plan query() | WP05 | P1 | Yes |
| T038       | Unit tests: plan immutability | WP05 | P1 | Yes |
| T039       | Implement CompositePlan.iterator() | WP06 | P2 | No |
| T040       | Implement composite iterator | WP06 | P2 | No |
| T041       | Implement execution ordering | WP06 | P2 | No |
| T042       | Implement data flow materialization | WP06 | P2 | No |
| T043       | Implement result combination | WP06 | P2 | No |
| T044       | Integration tests: composite execution | WP06 | P2 | Yes |
| T045       | Integration tests: execution ordering | WP06 | P2 | Yes |
| T046       | Integration tests: data flow | WP06 | P2 | Yes |
| T047       | Implement AST pattern suitability | WP07 | P2 | No |
| T048       | Implement heuristic probing | WP07 | P2 | No |
| T049       | Ensure priority order | WP07 | P2 | No |
| T050       | Handle edge case: no walker found | WP07 | P2 | No |
| T051       | Handle edge case: multiple walkers | WP07 | P2 | No |
| T052       | Handle edge case: walker declines | WP07 | P2 | No |
| T053       | Integration tests: priority order | WP07 | P2 | Yes |
| T054       | Integration tests: edge cases | WP07 | P2 | Yes |
| T055       | Integration tests: end-to-end | WP08 | P2 | Yes |
| T056       | Integration tests: composite execution | WP08 | P2 | Yes |
| T057       | Integration tests: real walkers | WP08 | P2 | Yes |
| T058       | Code cleanup and refactoring | WP08 | P2 | Yes |
| T059       | Documentation updates | WP08 | P2 | Yes |
| T060       | Validate quickstart scenario | WP08 | P2 | Yes |
| T061       | Performance validation | WP08 | P2 | Yes |

